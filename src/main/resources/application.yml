spring:
  datasource:
    url: jdbc:mysql://localhost:3306/incidentflow?createDatabaseIfNotExist=true&serverTimezone=UTC
    username: root
    password: password

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

server:
  port: 8080